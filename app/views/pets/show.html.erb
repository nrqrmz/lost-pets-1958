<h1><%= @pet.name %></h1>
<h2><%= @pet.address %></h2>
<h2><%= @pet.days_ago %> on <%= @pet.found_on.strftime("%B %d, %Y") %></h2>
<h3><%= @pet.species %></h3>


<%= link_to 'Edit', edit_pet_path(@pet) %>
<%= link_to 'Delete', pet_path(@pet), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>

<hr>

<%= link_to 'Back to pets', pets_path %>
